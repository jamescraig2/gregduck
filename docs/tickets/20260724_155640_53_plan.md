# Implementation Plan: feat: Set Interactive Map as home page and enhance map UI usability (Issue #53)

## Executive Summary

This implementation plan details the steps to set the interactive map (`MapDashboard`) as the default home page (`/`) and enhance map UI usability. The enhancements include floating control overlays for recentering the camera on the user's GPS position via `@vis.gl/react-google-maps`, a quick capture Floating Action Button (FAB) that opens `CameraCaptureModal`, client-side species chip and search text filtering, persistent header navigation, and toast feedback for error/status notifications.

## Scope of Changes

| File Path                                                                                                                       | Action | Description                                                                                                                                                                 |
| :------------------------------------------------------------------------------------------------------------------------------ | :----- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `[app/page.tsx](file:///Users/jamescraig/gh/gregduck/app/page.tsx)`                                                             | Modify | Replace placeholder static headings with full-viewport interactive `MapDashboard`.                                                                                          |
| `[components/map/MapControlsOverlay.tsx](file:///Users/jamescraig/gh/gregduck/components/map/MapControlsOverlay.tsx)`           | Create | Glassmorphic overlay component for species filter chips, search input, recenter GPS FAB, quick camera capture FAB, and error toast feedback.                                |
| `[components/map/MapDashboard.tsx](file:///Users/jamescraig/gh/gregduck/components/map/MapDashboard.tsx)`                       | Modify | Integrate `MapControlsOverlay`, `useMap` hook for programmatic camera panning, client-side species/search filtering, camera modal triggering, and toast notification state. |
| `[tests/page.test.tsx](file:///Users/jamescraig/gh/gregduck/tests/page.test.tsx)`                                               | Modify | Update home page tests to verify `MapDashboard` rendering instead of deprecated text headings.                                                                              |
| `[tests/components/map/MapDashboard.test.tsx](file:///Users/jamescraig/gh/gregduck/tests/components/map/MapDashboard.test.tsx)` | Modify | Add unit tests for Recenter button camera panning, Quick Capture FAB modal opening, species chip/search filtering, and error toast display.                                 |

---

## Phase 1: Preparation & Configuration

1. **Overlay Styling & Component Interface Scaffolding**:
   - Define interface for `MapControlsOverlayProps`:
     ```typescript
     export interface MapControlsOverlayProps {
       onRecenter: () => void;
       onQuickCapture: () => void;
       selectedSpecies: string;
       onSelectSpecies: (species: string) => void;
       searchQuery: string;
       onSearchChange: (query: string) => void;
       toastMessage?: string | null;
       onCloseToast?: () => void;
     }
     ```
   - Utilize existing glassmorphic CSS utilities (`glass-panel`, Tailwind `bg-slate-900/80 backdrop-blur-md border border-slate-700/60`).

---

## Phase 2: Core Implementation

1. **Create `MapControlsOverlay.tsx`**:
   - File: `[components/map/MapControlsOverlay.tsx](file:///Users/jamescraig/gh/gregduck/components/map/MapControlsOverlay.tsx)`
   - Implement:
     - Top search bar & species filter chip bar (`All`, `Duck`, `Squirrel`, `Rabbit`, `Bird`, `Goose`).
     - Bottom-right Floating Action Buttons (FABs):
       - Recenter GPS button (`aria-label="Recenter location"`).
       - Camera Quick Capture FAB (`aria-label="Quick capture animal"`).
     - Floating toast alert for location/fetch error messages.

2. **Enhance `MapDashboard.tsx`**:
   - File: `[components/map/MapDashboard.tsx](file:///Users/jamescraig/gh/gregduck/components/map/MapDashboard.tsx#L1-L90)`
   - Signatures & Inner Logic:
     - Add `MapRecenterController` inner component wrapped by `APIProvider` to access `useMap()` hook.
     - Implement `handleRecenter()`:
       ```typescript
       const map = useMap();
       const handleRecenter = useCallback(() => {
         if (map && center) {
           map.panTo(center);
           map.setZoom(14);
         }
       }, [map, center]);
       ```
     - Add state for `selectedSpecies` (default `'all'`), `searchQuery` (default `''`), `isCameraOpen` (default `false`), and `toastMessage` (default `null`).
     - Filter `markers` array into `filteredMarkers` using species and search text matching.
     - Render `CameraCaptureModal` when `isCameraOpen` is true.
     - Catch `/api/markers` fetch errors and populate `toastMessage` (e.g., `"Failed to load markers. Please try again."`).

3. **Update Home Page (`app/page.tsx`)**:
   - File: `[app/page.tsx](file:///Users/jamescraig/gh/gregduck/app/page.tsx#L1-L11)`
   - Replace placeholder text with:
     ```tsx
     import { MapDashboard } from '@/components/map/MapDashboard';

     export default function Home() {
       return (
         <main className="relative w-full h-[calc(100vh-4rem)]">
           <MapDashboard />
         </main>
       );
     }
     ```

---

## Phase 3: Integration & Testing

1. **Update `tests/page.test.tsx`**:
   - File: `[tests/page.test.tsx](file:///Users/jamescraig/gh/gregduck/tests/page.test.tsx#L1-L22)`
   - Update tests to verify `/` renders `MapDashboard` (asserting `getByTestId('map')` or `getByTestId('map-dashboard')`).

2. **Enhance `tests/components/map/MapDashboard.test.tsx`**:
   - File: `[tests/components/map/MapDashboard.test.tsx](file:///Users/jamescraig/gh/gregduck/tests/components/map/MapDashboard.test.tsx#L50-L129)`
   - Add test cases:
     - `recenters map camera to user position when recenter button is clicked`
     - `opens CameraCaptureModal when quick capture FAB is clicked`
     - `filters markers client-side by selected species chip`
     - `filters markers client-side by search text query`
     - `displays error toast banner when /api/markers returns error`

3. **Run Test Verification**:
   - Command: `npm test`

---

## Phase 4: Rollout & Rollback Strategy

1. **Rollout Steps**:
   - Build and verify locally with `npm run build`.
   - Ensure all routes (`/`, `/map`, `/journal`) maintain consistent navigation and responsive layout.

2. **Rollback Plan**:
   - If UI issues arise, revert `app/page.tsx` and `components/map/MapDashboard.tsx` via git commit revert (`git checkout main -- app/page.tsx components/map/MapDashboard.tsx`).

---

## Verification Checklist

- [ ] Home page (`/`) renders `MapDashboard` in full viewport container (`h-[calc(100vh-4rem)]`).
- [ ] Recenter GPS button pans map camera back to user coordinates using `useMap()`.
- [ ] Quick capture FAB opens `CameraCaptureModal`.
- [ ] Species filter chips and search bar filter markers dynamically.
- [ ] Error toasts display when location or marker fetching fails.
- [ ] All unit and integration tests pass via `npm test`.

---

## Critique

**Verdict**: PASSED
**Critiqued at**: 2026-07-24T15:57:30Z
**Critical findings**: 0
**Advisory findings**: 0

### Validated

- 7 file/symbol claims verified
- 5 command/pattern claims verified
- 0 external URLs verified
